import { floorplan, booth, marker, story, vr } from "../../data/formTemplates"

export const initFormState = {
    floorplan: floorplan,
    booth: booth,
    vr: vr,
    marker: marker,
    story: story,
    file: null,
}