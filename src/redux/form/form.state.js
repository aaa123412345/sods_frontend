import { floorplan, booth, marker, story, game } from "../../data/formTemplates"

export const initFormState = {
    floorplan: floorplan,
    booth: booth,
    marker: marker,
    story: story,
    game: game,
}